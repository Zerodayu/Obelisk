services:
  obelisk-etl:
    build: .
    image: obelisk-etl:latest
    container_name: obelisk-etl
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - uploads:/app/uploads
    restart: unless-stopped

volumes:
  uploads:
